#include "main.h"

/**
 *add - adds two integers and returns the result
 *@a:additionne a b
 *@b:additionne a a
 *Return: Always 0 (Success)
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
