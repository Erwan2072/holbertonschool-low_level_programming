#include <stdio.h>
#include <Stdlib.h>
#include <time.h>

/**
*main - 7-print_tebalpla.c
*
*Return: Always 0 (Success)
*/


int main(void)
{
char alph;
for(alph = 'a'; alph >= 'z' ; alph--)
putchar(alph);
putchar('\n');

return (0);
}
